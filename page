<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPC IDE — Подробная справка по языку UPC</title>
    <style>
        :root {
            --primary-color: #2E86C1;
            --secondary-color: #2874A6;
            --accent-color: #3498DB;
            --light-bg: #F8F9F9;
            --dark-bg: #EAEDED;
            --code-bg: #F2F3F4;
            --border-color: #D6DBDF;
            --text-color: #2C3E50;
            --success-color: #28B463;
            --warning-color: #F39C12;
            --error-color: #E74C3C;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            background-color: var(--light-bg);
        }
        
        h1 {
            color: var(--primary-color);
            border-bottom: 2px solid var(--accent-color);
            padding-bottom: 10px;
            margin-bottom: 25px;
        }
        
        h2 {
            color: var(--secondary-color);
            margin-top: 30px;
            padding: 8px 15px;
            background-color: var(--dark-bg);
            border-left: 4px solid var(--accent-color);
            border-radius: 0 5px 5px 0;
        }
        
        h3 {
            color: var(--secondary-color);
            margin-top: 20px;
        }
        
        b {
            color: var(--primary-color);
        }
        
        code {
            font-family: 'Consolas', monospace;
            background-color: var(--code-bg);
            padding: 2px 5px;
            border-radius: 3px;
            font-size: 0.95em;
        }
        
        pre {
            background-color: var(--code-bg);
            padding: 15px;
            border-radius: 5px;
            overflow-x: auto;
            border-left: 3px solid var(--accent-color);
        }
        
        blockquote {
            border-left: 4px solid var(--accent-color);
            padding-left: 15px;
            margin-left: 0;
            color: #555;
            font-style: italic;
        }
        
        .command {
            background-color: white;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            border-top: 3px solid var(--accent-color);
        }
        
        .command-title {
            font-weight: bold;
            color: var(--primary-color);
            font-size: 1.1em;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }
        
        .command-title:before {
            content: "➤";
            margin-right: 10px;
            color: var(--accent-color);
        }
        
        .example {
            background-color: var(--dark-bg);
            padding: 10px 15px;
            border-radius: 5px;
            margin: 10px 0;
        }
        
        .example-title {
            font-weight: bold;
            color: var(--secondary-color);
            margin-bottom: 5px;
        }
        
        .tip-box {
            background-color: #EAF2F8;
            padding: 15px;
            border-radius: 5px;
            border-left: 4px solid var(--accent-color);
            margin: 20px 0;
        }
        
        .tip-title {
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }
        
        .tip-title:before {
            content: "💡";
            margin-right: 10px;
        }
        
        .footer {
            font-size: 0.9em;
            color: #7F8C8D;
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid var(--border-color);
        }
        
        .toc {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        
        .toc-title {
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 10px;
        }
        
        .toc-list {
            columns: 2;
            list-style-type: none;
            padding-left: 0;
        }
        
        .toc-list li {
            margin-bottom: 8px;
            break-inside: avoid;
        }
        
        .toc-list a {
            text-decoration: none;
            color: var(--text-color);
            transition: color 0.2s;
        }
        
        .toc-list a:hover {
            color: var(--accent-color);
            text-decoration: underline;
        }
        
        .command-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        
        @media (max-width: 768px) {
            .command-grid {
                grid-template-columns: 1fr;
            }
            
            .toc-list {
                columns: 1;
            }
        }
    </style>
</head>
<body>
    <h1>UPC IDE — Подробная справка по языку UPC</h1>
    
    <div class="tip-box">
        <div class="tip-title">Что такое UPC?</div>
        <p>UPC (Universal Patch Container) — это упрощённый язык для написания патчей к программам. С его помощью можно создавать скрипты, которые встраиваются в программы и изменяют их поведение, файлы или содержимое. Язык разработан для удобства и универсальности.</p>
    </div>
    
    <div class="toc">
        <div class="toc-title">Содержание</div>
        <ul class="toc-list">
            <li><a href="#basic-commands">Основные команды UPC</a></li>
            <li><a href="#advanced-example">Пример полного патча</a></li>
            <li><a href="#tips">Советы и рекомендации</a></li>
        </ul>
    </div>
    
    <h2 id="basic-commands">Основные команды UPC</h2>
    
    <div class="command-grid">
        <div class="command">
            <div class="command-title">patch</div>
            <p>Объявляет начало патча и его версию.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>patch 1.0</code>
                <p>Описывает патч версии 1.0.</p>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">description</div>
            <p>Описание патча (текст).</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>description "исправление ошибки загрузки"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">replace</div>
            <p><code>"файл.file"</code>, <code>"старый_текст"</code> to <code>"новый_текст"</code></p>
            <p>Заменяет в указанном файле все вхождения старого_текста на новый_текст. Если файл не указан, замена происходит в основном файле патча.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>replace "config.ini", "Hello" to "Привет"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">delete</div>
            <p><code>"файл.file"</code> <code>"текст"</code></p>
            <p>Удаляет указанную строку из указанного файла. Если файл не указан, удаление происходит в основном файле патча.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>delete "config.ini" "debug_mode=1"</code><br>
                или<br>
                <code>delete "debug_mode=1"</code> — удаление из основного файла.
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">copy</div>
            <p><code>источник</code> to <code>назначение</code></p>
            <p>Копирует файл.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>copy "backup.ini" to "config.ini"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">create</div>
            <p><code>путь_к_файлу</code></p>
            <p>Создаёт новый файл с содержимым, записанным в блоке.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <pre>create "new_script.py"
(
    print("привет мир")
    print("это новый скрипт")
)
ce</pre>
                <p>В скобках ( ) — построчно пишется содержимое файла. Команда <code>ce</code> — окончание блока создания.</p>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">rename</div>
            <p><code>старое_имя</code> to <code>новое_имя</code></p>
            <p>Переименовывает файл.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>rename "old.txt" to "new.txt"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">message</div>
            <p><code>"текст"</code></p>
            <p>Показывает пользователю сообщение во время применения патча.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>message "патч успешно установлен!"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">run</div>
            <p><code>команда</code></p>
            <p>Выполняет системную команду.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>run "python new_script.py"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">if</div>
            <p><code>условие</code> then <code>команда</code></p>
            <p>Условное выполнение команды.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>if file_exists "config.ini" then message "конфигурация найдена"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">embed</div>
            <p><code>[(create)] "имя_файла" type base64</code></p>
            <p>Встраивает бинарный файл в патч в формате base64. Если указан флаг <code>create</code>, то создаёт этот файл при применении патча.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <pre>embed create "image.png" type base64
(
iVBORw0KGgoAAAANSUhEUgAA...
)
ce</pre>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">start</div>
            <p><code>путь_к_файлу_или_команда</code></p>
            <p>Запускает указанный файл или команду в системе.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>start "hello.py"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">move</div>
            <p><code>источник</code> to <code>назначение</code></p>
            <p>Перемещает файл или папку.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>move "old_folder/file.txt" to "new_folder/file.txt"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">mkdir</div>
            <p><code>путь_к_каталогу</code></p>
            <p>Создаёт каталог (включая промежуточные папки).</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>mkdir "logs/archive/2025-07-04"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">rmdir</div>
            <p><code>путь_к_каталогу</code></p>
            <p>Удаляет пустой каталог.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>rmdir "temp/old_build"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">zip</div>
            <p><code>имя_архива.zip</code> add <code>файл_или_папка</code> [add <code>файл_или_папка</code>]...</p>
            <p>Создаёт zip-архив и добавляет в него файлы или папки.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>zip "release.zip" add "bin/" add "docs/readme.txt"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">unzip</div>
            <p><code>архив.zip</code> to <code>папка_назначения</code></p>
            <p>Распаковывает zip-архив в указанную папку.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>unzip "assets.zip" to "assets/"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">download</div>
            <p><code>url</code> to <code>путь_к_файлу</code></p>
            <p>Скачивает файл из интернета.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>download "https://example.com/tool.exe" to "tools/tool.exe"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">checksum</div>
            <p><code>файл</code> md5|sha256 <code>"контрольная_сумма"</code></p>
            <p>Проверяет контрольную сумму файла.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>checksum "data.bin" md5 "5d41402abc4b2a76b9719d911017c592"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">wait</div>
            <p><code>число_миллисекунд</code></p>
            <p>Пауза в выполнении патча.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>wait 2000</code> <i># ждать 2 секунды</i>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">env</div>
            <p><code>set|get переменная [значение]</code></p>
            <p>Управление переменными окружения.</p>
            <div class="example">
                <div class="example-title">Пример установки:</div>
                <code>env set "PATH" "%PATH%;C:\tools"</code><br>
                <div class="example-title">Пример получения:</div>
                <code>env get "JAVA_HOME"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">log</div>
            <p><code>файл_лога</code> <code>"текст"</code></p>
            <p>Записывает строку в лог-файл.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>log "patch.log" "начало патча"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">backup</div>
            <p><code>файл</code></p>
            <p>Создаёт резервную копию файла перед изменением.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <code>backup "config.ini"</code>
            </div>
        </div>
        
        <div class="command">
            <div class="command-title">exec_if</div>
            <p><code>условие</code> ( <code>блок_команд</code> )</p>
            <p>Выполняет блок команд, если условие истинно.</p>
            <div class="example">
                <div class="example-title">Пример:</div>
                <pre>exec_if file_exists "plugin.dll" (
  message "плагин уже установлен"
  stop
)</pre>
            </div>
        </div>
    </div>
    
    <h2 id="advanced-example">Пример полного патча с расширенными командами</h2>
    
    <pre>patch 1.0
description "пример расширенного патча с заменой, созданием и embed"

replace "config.ini", "Hello" to "Привет"

create "hello.py"
(
print("привет мир!")
)
ce

embed create "image.png" type base64
(
iVBORw0KGgoAAAANSUhEUgAA...
)
ce

message "патч успешно применён!"

move "old_folder/file.txt" to "new_folder/file.txt"
mkdir "logs/archive/2025-07-04"
rmdir "temp/old_build"

zip "release.zip" add "bin/" add "docs/readme.txt"
unzip "assets.zip" to "assets/"

download "https://example.com/tool.exe" to "tools/tool.exe"
checksum "tools/tool.exe" md5 "5d41402abc4b2a76b9719d911017c592"

wait 2000

env set "PATH" "%PATH%;C:\tools"
env get "JAVA_HOME"

log "patch.log" "начало патча"
backup "config.ini"

exec_if file_exists "plugin.dll" (
  message "плагин уже установлен"
  stop
)</pre>
    
    <h2 id="tips">Советы и рекомендации</h2>
    
    <div class="tip-box">
        <ul>
            <li><b>Пишите команды с маленьких букв</b> — это облегчает чтение и парсинг.</li>
            <li><b>Текст внутри кавычек ""</b> — чувствителен к регистру и должен быть точным.</li>
            <li><b>Для больших файлов</b> используйте команду <code>embed</code> с base64, чтобы встроить бинарные данные.</li>
            <li><b>Всегда проверяйте синтаксис</b> перед применением патча.</li>
            <li><b>Используйте <code>backup</code></b>, чтобы сохранить оригиналы файлов перед изменениями.</li>
            <li><b>Проверяйте условия</b> в <code>if</code> и <code>exec_if</code>, чтобы избежать ошибок при применении патчей.</li>
        </ul>
    </div>
    
    <div class="footer">
        UPC IDE — помощник для создания патчей. Все права НЕ защищены, хехе.
    </div>
</body>
</html>
